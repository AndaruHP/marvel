import React from "react";

const CharacterList = (props) => {
  const ratio = "potrait_incredible";
  return (
    <div></div>
    // <li>
    //   <h2>{props.name}</h2>
    //   <p>{props.description}</p>
    //   <img src={`${props.path}/${ratio}.${props.extension}`} alt={props.name} />
    // </li>
  );
};

export default CharacterList;
